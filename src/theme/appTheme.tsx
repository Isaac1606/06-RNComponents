import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 20,
        marginTop: 10
    },
    title: {
        fontSize: 35,
        fontWeight: 'bold',
        color: 'black'
    }
});